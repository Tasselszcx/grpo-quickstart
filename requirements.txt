# =============================================================================
# GRPO Quickstart — Python 依赖
# 通过 setup.sh 自动安装，这里列出具体版本供参考和手动安装
# =============================================================================

# ── 深度学习核心 ──────────────────────────────────────────────────────────────
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1
# PyTorch 需要指定 CUDA 版本：
# pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu124

# ── RL 训练框架 ───────────────────────────────────────────────────────────────
verl                          # volcengine veRL，GRPO 的训练框架
vllm==0.8.5                   # 高速 rollout 推理引擎
flash-attn                    # Flash Attention 2，加速训练（需要编译）
ray[default]>=2.35.0          # veRL 的分布式后端

# ── 模型和数据 ────────────────────────────────────────────────────────────────
transformers>=4.47.0
datasets>=2.20.0
accelerate>=1.0.0
tokenizers>=0.20.0
huggingface_hub>=0.26.0
pandas>=2.0.0
pyarrow>=15.0.0

# ── 实验跟踪 ──────────────────────────────────────────────────────────────────
wandb
tensorboard
tensorboardX

# ── 可视化 ────────────────────────────────────────────────────────────────────
matplotlib
seaborn
